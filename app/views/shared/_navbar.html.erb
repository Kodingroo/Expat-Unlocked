<nav id="navigation" class="">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar__left">
      <a href="/" class="navbar-link" style="font-size: 36px; font-family: Audiowide;">
        b
      </a>
    </div>

    <div id="navigation__right">
      <% if user_signed_in? %>
        <div><%= link_to "Profile", profile_path %></div>
        <div><%= link_to "My Documents", user_documents_path(current_user)%></div>
        <div><%= link_to "Log out", destroy_user_session_path, method: :delete %></div>
      <% else %>
        <%= link_to "Sign in", new_user_session_path %>
      <% end %>
    </div>
    <div id="navigation__hamburger">
      <i class="fas fa-bars"></i>
      <div class="navigation__drop">
          <% if user_signed_in? %>
            <div><%= link_to "Profile", profile_path(current_user)%></div>
            <div><%= link_to "My Documents", user_documents_path(current_user)%></div>
            <div><%= link_to "Log out", destroy_user_session_path, method: :delete %></div>
          <% else %>
            <%= link_to "Sign in", new_user_session_path %>
          <% end %>
        </div>
    </div>

  </div>
</nav>
